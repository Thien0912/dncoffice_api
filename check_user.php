<?php
define('BASEPATH', true);
require 'index.php'; // This might not work to boot CI manually

// Let's try to just dump the ql_nguoi_dung table for ID 635 if possible
// or use the CI instance if we can get it.

// Actually, I'll just check if I can find any obvious reason in the code first.
